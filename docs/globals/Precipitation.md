# global Precipitation

---

```lua
function Precipitation() ->  Precipitation {
    dataBlock = (PrecipitationData|SceneObject),
    numDrops = number,
    numOfDrops = number,
    splashSize = number,
    splashMS = number,
    animateSplashes = number,
    boxWidth = number,
    boxHeight = number,
    dropSize = number,
    doCollision = boolean,
    hitVehicles = boolean,
    rotateWithCamVel = boolean,
    followCam = boolean,
    useWind = boolean,
    minSpeed = number,
    maxSpeed = number,
    minMass = number,
    maxMass = number,
}
```






Creates a new Precipitation object

