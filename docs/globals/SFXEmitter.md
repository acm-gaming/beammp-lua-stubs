# global SFXEmitter

---

```lua
function SFXEmitter() ->  SFXEmitter {
    track = string,
    fileName = string,
    playOnAdd = boolean,
    isLooping = boolean,
    volume = number,
    isStreaming = boolean,
    is3D = boolean,
    canSave = boolean,
    useTrackDescriptionOnly = boolean,
    sourceGroup = string,
}
```






Creates a new SFXEmitter object

